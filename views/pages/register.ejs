<!DOCTYPE html>
<html lang="de" data-theme="helpbaerai">
<head>
    <%- include('../partials/head') %>
    <title>Registrierung | HelpBärAI</title>
</head>
<body class="bg-base-100 text-base-content flex flex-col min-h-screen">
    <%- include('../partials/header') %>
    
    <main class="flex-grow container mx-auto px-4 py-12">
        <div class="max-w-md mx-auto bg-base-200 p-8 rounded-lg shadow-lg">
            <h1 class="text-3xl font-bold mb-8 text-center text-primary">Registrierung</h1>
            <% if (locals.error) { %>
                <div class="alert alert-error mb-4">
                    <%= error %>
                </div>
            <% } %>
            <% if (locals.success) { %>
                <div class="alert alert-success mb-4">
                    <%= success %>
                </div>
            <% } %>
            <form action="/auth/register" method="POST" class="space-y-6" id="registrationForm" novalidate>
                <div>
                    <label for="firstname" class="block text-sm font-medium mb-2 text-base-content">Vorname</label>
                    <input type="text" id="firstname" name="firstname" required
                           class="w-full px-3 py-2 border border-base-300 rounded-md shadow-sm bg-base-100 text-base-content focus:outline-none focus:ring-2 focus:ring-primary focus:border-primary">
                </div>
                <div>
                    <label for="email" class="block text-sm font-medium mb-2 text-base-content">E-Mail</label>
                    <input type="email" id="email" name="email" required
                           class="w-full px-3 py-2 border border-base-300 rounded-md shadow-sm bg-base-100 text-base-content focus:outline-none focus:ring-2 focus:ring-primary focus:border-primary">
                </div>
                <div>
                    <label for="password" class="block text-sm font-medium mb-2 text-base-content">Passwort</label>
                    <input type="password" id="password" name="password" required
                           class="w-full px-3 py-2 border border-base-300 rounded-md shadow-sm bg-base-100 text-base-content focus:outline-none focus:ring-2 focus:ring-primary focus:border-primary">
                </div>
                <div>
                    <label for="password_confirmation" class="block text-sm font-medium mb-2 text-base-content">Passwort wiederholen</label>
                    <input type="password" id="password_confirmation" name="password_confirmation" required
                           class="w-full px-3 py-2 border border-base-300 rounded-md shadow-sm bg-base-100 text-base-content focus:outline-none focus:ring-2 focus:ring-primary focus:border-primary">
                </div>
                <div>
                    <button type="submit" class="w-full btn btn-primary text-primary-content text-lg py-3 transition-colors duration-300 hover:bg-primary-focus">
                        Registrieren
                    </button>
                </div>
            </form>
            <p class="mt-6 text-center text-sm text-base-content">
                Bereits registriert? <a href="/auth/login" class="text-primary hover:underline font-medium">Zum Login</a>
            </p>
        </div>

        <div class="max-w-md mx-auto bg-base-200 p-8 rounded-lg shadow-lg">
            <h1 class="text-3xl font-bold mb-8">Hinweis</h1>
            <p class="mb-4">
                Deine Angaben werden zur Anmeldung und Nutzung von HelpBärAI verwendet und zusätzlich erhälst exklusive Informationen sowie weiterführenden Angebote aus unserem AI Service.
            </p>
            <p class="mb-6">
                <strong class="text-error">Deine Daten, deine Verantwortung:</strong> Bitte nutze bei deinen Tests keine sensiblen Daten, es handelt sich hier um eine Demo Plattform, die mit den öffentlich verfügbaren Sprachemodellen von OpenAI, Anthropic, Google Gemini und mit der Automatisierungsplattform Make.com kommuniziert. Übermittelte Daten werden in Logs gespeichert und können von unseren Entwicklern ausgewertet werden.
            </p>
        </div>
    </main>

    <%- include('../partials/footer') %>

    <script src="/js/register.js"></script>
</body>
</html>